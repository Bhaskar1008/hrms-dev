const initialState = {}

const filterDataReducer = (state = initialState, { type, payload }) => {
  return 
  // switch (type) {

  // case first:
  //   return { ...state, ...payload }

  // default:
  //   return state
  // }
}

export default filterDataReducer